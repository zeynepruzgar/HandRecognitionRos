# Client Hand Control Dependencies
# NO ROS2 DEPENDENCIES

# Core
numpy>=1.24.0
opencv-python>=4.8.0

# Hand detection
mediapipe==0.10.21

# Keyboard input is read via cv2.waitKey on the preview window (no extra dep).

# Transport: plain UDP datagrams to the rover. socket is stdlib, no dependency.
